repos:
  - repo: local
    hooks:
      - id: typst-build
        name: Compile Typst to PDF
        entry: bash -c 'typst compile main.typ main.pdf'
        language: system
        files: \.typ$
        pass_filenames: false
